# Настройка Cursor AI для проекта Difference Machine

## ✅ Правила созданы

Правила для Cursor AI созданы в двух форматах для максимальной совместимости:

1. **`.cursorrules`** (корень проекта) - классический формат (9.1 KB)
2. **`.cursor/rules/*.mdc`** - современный формат с разделением по категориям

## 📁 Структура правил

```
.cursor/
└── rules/
    ├── code-style.mdc          # PEP 8, форматирование, type hints
    ├── logging.mdc             # Логирование (никогда print!)
    ├── architecture.mdc        # Архитектурные паттерны
    ├── error-handling.mdc      # Обработка ошибок
    └── common-mistakes.mdc     # Частые ошибки и чеклист
```

## 🚀 Автоматическое подключение

Cursor AI автоматически обнаружит и применит правила из обоих мест:
- ✅ Файл `.cursorrules` в корне проекта
- ✅ Папка `.cursor/rules/` с файлами `.mdc`

**Никаких дополнительных действий не требуется!**

## 🔍 Проверка подключения

1. **Перезапустите Cursor** (если он был открыт при создании правил)
2. Откройте любой Python файл в проекте
3. Попробуйте запросить у Cursor AI:
   ```
   Создай функцию для проверки существования репозитория
   ```
4. Cursor должен:
   - ✅ Использовать `logger` вместо `print()`
   - ✅ Добавить type hints
   - ✅ Добавить docstring в формате Google
   - ✅ Использовать `Path` вместо строк
   - ✅ Следовать PEP 8 (макс. 120 символов)

## 📋 Содержание правил

### 1. Code Style (code-style.mdc)
- PEP 8 соответствие
- Форматирование (отступы, пробелы, пустые строки)
- Type hints обязательны
- Google-style docstrings

### 2. Logging (logging.mdc)
- **НИКОГДА не использовать `print()`**
- Всегда использовать `logger` с правильными уровнями
- Логирование исключений с `exc_info=True`

### 3. Architecture (architecture.mdc)
- Структура модулей проекта
- Паттерны работы с БД
- Разрешение путей репозитория
- Работа с файлами через `pathlib.Path`

### 4. Error Handling (error-handling.mdc)
- Типы исключений
- Паттерны возврата значений
- Обработка ошибок БД и файлов

### 5. Common Mistakes (common-mistakes.mdc)
- 10 критических правил
- Чеклист для code review

## 🛠 Ручная проверка

```bash
# Проверить наличие файлов
ls -la .cursorrules
ls -la .cursor/rules/

# Посмотреть список правил
find .cursor/rules -name "*.mdc"

# Просмотреть правила
cat .cursorrules | head -30
cat .cursor/rules/code-style.mdc
```

## 🔄 Обновление правил

Если нужно обновить правила:

1. Отредактируйте нужный файл в `.cursor/rules/` или `.cursorrules`
2. Перезапустите Cursor или перезагрузите проект (Cmd+R)
3. Cursor автоматически подхватит изменения

## 🎯 Примеры использования

### Хороший код (согласно правилам):
```python
import logging
from pathlib import Path
from typing import Optional

logger = logging.getLogger(__name__)

def check_repository(path: Path) -> Optional[Path]:
    """
    Check if path is a valid Forester repository.

    Args:
        path: Path to check

    Returns:
        Repository path if valid, None otherwise
    """
    dfm_dir = path / ".DFM"
    if dfm_dir.exists():
        return path
    logger.warning(f"Not a repository: {path}")
    return None
```

### Плохой код (нарушает правила):
```python
def check_repository(path):  # ❌ Нет type hints
    if (path / ".DFM").exists():  # ❌ Использует Path, но без импорта
        return path
    print(f"Not a repository: {path}")  # ❌ print() вместо logger
    return None  # ❌ Нет docstring
```

## 📚 Дополнительная информация

### Где находятся файлы:
```
/Users/dmitry/Library/Application Support/Blender/5.0/extensions/user_default/difference_machine/
├── .cursorrules                    # Основной файл правил
└── .cursor/
    └── rules/
        ├── code-style.mdc
        ├── logging.mdc
        ├── architecture.mdc
        ├── error-handling.mdc
        └── common-mistakes.mdc
```

### Приоритеты правил:
Правила применяются в порядке приоритета (1-5, где 1 - высший):
1. Code Style
2. Logging
3. Architecture
4. Error Handling
5. Common Mistakes

## ⚠️ Troubleshooting

### Cursor не использует правила
1. Убедитесь, что файлы находятся в правильных местах
2. Перезапустите Cursor полностью
3. Проверьте версию Cursor (обновите до последней)
4. Откройте проект через File → Open Folder

### Правила не применяются
1. Проверьте, что файлы `.mdc` имеют правильный формат (YAML front matter)
2. Убедитесь, что `globs` указаны правильно
3. Проверьте `alwaysApply: true` в каждом файле

### Нужны разные правила для разных модулей
Создайте дополнительные файлы с другими `globs`:
- `operators-rules.mdc` с `globs: ["operators/**/*.py"]`
- `tests-rules.mdc` с `globs: ["**/test_*.py"]`

## ✨ Готово!

Правила настроены и готовы к использованию. Cursor AI теперь будет:
- ✅ Следовать PEP 8
- ✅ Использовать logger вместо print()
- ✅ Применять правильные архитектурные паттерны
- ✅ Помогать избегать частых ошибок
